[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
  # @netlify/blobs is built into Netlify's runtime — tell esbuild not to bundle it
  external_node_modules = ["@netlify/blobs"]
