[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# SPA routing — serve index.html for all non-file paths (enables /xero-callback)
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
