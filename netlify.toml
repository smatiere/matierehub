[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
  included_files = []

# @netlify/blobs is built into Netlify's function runtime — no package.json needed
